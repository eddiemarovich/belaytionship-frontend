import React from 'react'
import { Text, TouchableOpacity, Image } from 'react-native'

const Carabiner = ({ loggedIn, onPress }) => {
  const { carabinerMainStyle, carabinerSideStyle } = styles
  return (
    <TouchableOpacity onPress= {onPress}>
      <Image style 
    </TouchableOpacity>
  )
}
